[package]
name = "rmap-config"
version.workspace = true
edition.workspace = true
license.workspace = true

[[bin]]
name = "rmap-config"
path = "src/main.rs"

[dependencies]
rmap-core = { path = "../rmap-core" }
# slint later for GUI (step 12)
# For prototype, can be CLI stub first, then Slint
